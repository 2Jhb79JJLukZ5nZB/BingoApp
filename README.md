# ビンゴボード

ビンゴで呼ばれた番号を記録する、GitHub Pages向けのシンプルなウェブアプリです。

## 機能

- 中央のカードから独立した番号ボードを開く
- 対応ブラウザーでは番号ボードを常に最前面に表示
- 1〜75の番号をEnterキーまたはボタンで追加
- 呼ばれた順番を3列で表示
- 各番号の×ボタンから削除
- ブラウザー内に番号を自動保存
- PC・スマートフォンに対応

常時最前面表示にはDocument Picture-in-Picture APIを使用します。未対応ブラウザーでは通常のポップアップウィンドウで表示します。

## ローカルでの確認

外部ライブラリは使用していません。`index.html`をブラウザーで開くか、任意の静的ファイルサーバーでこのフォルダーを配信してください。

`main`ブランチへプッシュすると、GitHub ActionsがGitHub Pagesへ自動公開します。
